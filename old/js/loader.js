//display full-screen window loader gif while page loads
$(window).load(function() {
	//fade out when page completes loading
	$(".loading-spinner").fadeOut("slow");
	//$("#lrossi").fadeIn("slow");
});
